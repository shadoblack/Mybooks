import { PipeMayusPipe } from './pipe-mayus.pipe';

describe('PipeMayusPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeMayusPipe();
    expect(pipe).toBeTruthy();
  });
});
